pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo "starting build"
            }
        }
        
        stage("final") {
            steps {
                echo "starting final"
            }
        }
    }
}