language: node_js

services:
  - docker

before_install:
  - docker build -t my_domain/project_name -f ./Dockerfile .
  - docker save my_domain/project_name | gzip -c > app_image.tar.gz
  - chmod +r app_image.tar.gz

script:
  - echo "Ola travis"